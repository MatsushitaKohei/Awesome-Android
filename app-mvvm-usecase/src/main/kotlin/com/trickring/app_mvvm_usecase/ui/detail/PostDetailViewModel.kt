package com.trickring.app_mvvm_usecase.ui.detail

import androidx.lifecycle.AndroidViewModel
import com.trickring.app_mvvm_usecase.App

/**
 * Post Detail Screen's ViewModel. [AndroidViewModel] subclass.
 */
class PostDetailViewModel(app: App) : AndroidViewModel(app) {

    /**
     * Initialize
     *
     * @param args Arguments
     */
    fun init(args: PostDetailFragmentArgs) {
    }
}
